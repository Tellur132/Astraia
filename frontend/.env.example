# Frontend will proxy /api to the FastAPI backend when using `npm run dev`.
# Override when building a static bundle served from another host.
# VITE_API_BASE_URL=http://localhost:8000
# VITE_API_PROXY_TARGET=http://localhost:8000
